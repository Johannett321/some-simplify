version: '3.8'

services:
  db:
    image: postgres:16
    container_name: appweb.lowerappname-db
    environment:
      POSTGRES_USER: appweb.lowerappnameuser
      POSTGRES_PASSWORD: password
      POSTGRES_DB: appweb.lowerappnamedb
    volumes:
      - appweb.lowerappname_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  appweb.lowerappname_data: